node_modules/
.next/

# Local env files (secrets) — không bao giờ commit
.env
.env*.local

# Playwright
/playwright-report/
/playwright-report-smoke/
/test-results/
/.lighthouseci/
/tests/.auth/
*.webm
.vercel

# tuvi-engine build artifacts
tuvi-engine/dist/
tuvi-engine/coverage/
tsconfig.tsbuildinfo

# Claude Code / harness local state
# Ngoại lệ DUY NHẤT: settings.json phải theo repo thì MCP server `ga4` khai trong
# .mcp.json mới được duyệt sẵn ở mọi phiên (container phiên clone lại từ đầu).
# Mọi thứ khác trong .claude/ vẫn là state cục bộ — đừng commit.
.claude/*
!.claude/settings.json

# Local data, generated outputs & scratch
/qr/
/scripts/out/
/scripts/.gsc-verify-token.json
/sach/
/docs/miniapp-tool-inventory.md
/sample-laso-*.html
