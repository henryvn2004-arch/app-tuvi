# J6 + J7 — Người quay lại · Bán chéo sau report

Phương pháp: chủ yếu code-review (không có tài khoản thật đã đăng nhập). LIVE
chỉ chạy được ở trạng thái khách vô danh, mobile 390px, qua
`scripts/out/ux/j6j7-check.mjs` (Playwright + `scripts/ux/harness.mjs`).
Ảnh: `scripts/out/ux/shots/j6-*.png`, `j7-01-rail-after-m.png`.

## ĐÃ XÁC NHẬN

### [P1] Prod đang phục vụ bản CŨ của `/app/tro-chuyen` — dính lại 2 trong 3 tên đã bị "cấm" hôm nay
- Thiết bị: mobile 390px. Bước: `curl https://www.tuviminhbao.com/app/tro-chuyen`
  (khách vô danh), so với `public/app-tro-chuyen.html` trong repo.
- Ảnh: `scripts/out/ux/shots/j6-02-trochuyen-m.png` (= `j7-01-rail-after-m.png`).
- Trang live hiện `<title>`/tiêu đề "Trò chuyện", phụ đề "Mọi hội thoại với
  **Luận Đường**, gộp từ mọi công cụ" và empty-state "...hỏi **trợ lý**." —
  đúng 3 trong 5 tên cũ mà `docs/UX-AUDIT-PLAN.md` liệt là vấn đề ("ít nhất 5
  tên cho cửa chat"). Repo đã sửa đúng thành "Hỏi Thầy" ở `public/app-tro-chuyen.html:6,37`
  bởi commit `5d4f62c` ("Thống nhất tên cửa chat: Hỏi Thầy ở mọi chữ hiển thị")
  — commit này là HEAD hiện tại của nhánh đang review, tức bản deploy trên
  prod đang CHẬM HƠN repo, chưa chạy commit rename.
- Nguyên nhân gốc: không phải lỗi code — lệch giữa HEAD của nhánh review và
  bản đã deploy lên Vercel prod tại thời điểm audit (2026-09-26).
- Gợi ý: xác nhận `5d4f62c` đã lên đúng deployment prod (Vercel dashboard/`vercel_get_deployment`)
  trước khi coi việc "Một tên cho cửa chat" (W3) là xong; nếu đã deploy mà vẫn
  thấy bản cũ thì kiểm cache CDN edge cho `/app/tro-chuyen`.
- Không kiểm được J7 điểm 4 (gợi ý report khi hỏi "vận hạn năm sau") qua route
  này: `/app/tro-chuyen` là hộp thư DANH SÁCH hội thoại (không có ô nhập), rail
  chat thật nằm ở route khác (`/app` hoặc trong một tool) — script LIVE đã thử
  điền `#chatInput` ở trang này là sai mục tiêu của tôi, không phải lỗi site;
  không lặp lại kịp trong ngân sách phiên này. Để NGỎ, cần J1/J2 hoặc lượt sau
  xác nhận bằng đúng route rail.

### [Nhẹ] `public/auth.js:548` vẫn trỏ thẳng `/profile.html` thay vì `/app/ho-so`
- Menu thả xuống avatar (`auth.js`, `paintNav`) có mục "Hồ sơ của tôi" với
  `href="/profile.html"`. File này đã xoá (kiểm `ls public/profile.html` →
  không còn), `next.config.*:247` có redirect 308 `/profile.html → /app/ho-so`
  nên KHÔNG vỡ (chỉ tốn một round-trip redirect thêm).
- Đối chiếu: `lib/onboarding/tasks.ts:150` đã cố ý sửa đúng thành
  `/app/tai-khoan#ketnoi` kèm chú thích giải thích lý do — cùng đợt dọn dẹp
  không chạm tới `auth.js`. Có thể `auth.js` là nav cũ ít dùng (song song
  `public/nav.js` mới) — chưa xác minh trang nào còn nạp `auth.js` cho nav này.
- Gợi ý sửa tối thiểu: đổi `href="/profile.html"` → `href="/app/ho-so#account"`
  ở `auth.js:548`, một dòng.

## NGHI NGỜ / GAP ĐÃ BIẾT TRƯỚC (không phải phát hiện mới — ghi lại để khỏi trùng báo cáo khác)

- **`goi_y_san_pham` chỉ biết 2 report** (`laso`, `chu-trinh-cuoc-doi`,
  `lib/tools/suggest-tool.ts:146-156`) — đã xác nhận đúng như
  `docs/UX-AUDIT-PLAN.md` mục C1 mô tả. Bảng chủ đề→report (mục 3 kế hoạch)
  CHƯA tồn tại; đây là gap đã biết, không phải phát hiện mới.
- **`maybeShowUpsell` (`public/shell.js:2649`) luôn mời "Luận Giải"** bất kể
  chủ đề vừa hỏi (tình duyên/con cái/công việc) — khớp mục C2 đã ghi trong
  plan. Không phải phát hiện mới.
- **`report-delivery.js` không có khối "bước tiếp theo"** sau khi tải PDF /
  gửi email — `runSend`/nút PDF chỉ đổi trạng thái nút, không chèn gợi ý report
  khác hay câu hỏi mở. Khớp mục C3 đã ghi trong plan ("nhận report xong là hết
  đường"). Không phải phát hiện mới.

## Điểm tốt (để không sửa nhầm chỗ đang ổn)

- `public/app-tai-khoan.html` (J6): empty-state khách chưa đăng nhập rõ ràng —
  icon đơn sắc (`data-icon="sparkles"`), một câu giải thích, một CTA
  "Đăng Nhập / Đăng Ký", không emoji màu, không nhắc "AI". Ảnh:
  `scripts/out/ux/shots/j6-01-hoso-m.png`.
- `public/shell.js` `renderSidebar()` (dòng 581-654): khối "Lá số đã lưu" và
  "Công cụ yêu thích" dựng khung `hidden` trước, chỉ hiện khi có dữ liệu thật
  (`loadSidebarCharts`/`loadSidebarFavTools`) — không doạ khách vãng lai bằng
  khung rỗng, không có số giả kiểu "online" ở phần này.
- Tên "Hỏi Thầy" nhất quán ở MỌI chữ hiển thị trong `shell.js` (tab bar, tiêu
  đề rail, modal chia sẻ) — chỉ còn sót ở COMMENT nội bộ ("Trợ lý Luận Đường"),
  không ảnh hưởng người dùng.
- `showToolSuggest`/`.tool-suggest` (`shell.js:4767`, CSS `shell.css:498-510`)
  không hiện giá (đúng luật DESIGN.md "thẻ gợi ý không có giá"), style dùng
  token màu (`--navy`, `--gold-bright`) chứ không hard-code, không che khuất
  nội dung — chèn vào dòng chảy chat, không phải overlay.
- `lib/tools/suggest-tool.ts`: `resolveToolSuggestion` từ chối dựng thẻ khi
  thiếu `app_path` hay đang mở chính tool đó — tránh dẫn vào 404 hoặc gợi ý vô
  nghĩa; đúng nguyên tắc "thà im lặng còn hơn chỉ sai".

## Việc chưa làm được trong ngân sách phiên

- Chưa xác nhận trực tiếp bằng ảnh việc thẻ gợi ý report (`goi_y_san_pham`)
  THẬT SỰ hiện ra khi hỏi rail về "vận hạn năm sau" (J7.4) — cần vào đúng route
  có ô nhập (`/app` hoặc một tool cụ thể), không phải `/app/tro-chuyen`.
